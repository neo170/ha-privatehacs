# Release Workflow

For a release request, invoke `./scripts/Release.ps1` directly.

Do not run Git status, diff, tag, log, remote, or release checks before or after it.
Do not run the script with `-WhatIf`.